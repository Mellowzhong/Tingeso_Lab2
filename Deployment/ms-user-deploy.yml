apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-microservice-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-microservice-deployment
  template:
    metadata:
      labels:
        app: user-microservice-deployment
    spec:
      containers:
        - name: user-microservice-container
          image: mellow03/user-microservice:latest
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "0"
            # Variables desde ConfigMap (no sensibles)
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-map # Nombre del ConfigMap definido anteriormente.
                  key: DB_HOST_USER

            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-map # Nombre del ConfigMap definido anteriormente.
                  key: DB_PORT_USER

            - name: DB_DBNAME
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-map # Nombre del ConfigMap definido anteriormente.
                  key: DB_DBNAME_USER

            # Variables desde Secret (sensibles)
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials # Nombre del Secret definido anteriormente.
                  key: DB_USER_USER_DB

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials # Nombre del Secret definido anteriormente.
                  key: DB_PASSWORD_USER_DB