server:
  port: 8081
spring:
  application:
    name: backend-config
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Mellowzhong/Tingeso_Lab2
          search-paths: "config-data"