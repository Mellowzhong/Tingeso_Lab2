server:
  port: 8888
  address: 0.0.0.0
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mellowzhong/Tingeso_Lab2
          default-label: main
          search-paths: config-data