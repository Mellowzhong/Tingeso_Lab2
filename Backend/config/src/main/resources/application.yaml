server:
  port: 8081
spring:
  application:
    name: backend-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mellowzhong/Tingeso_Lab2
          default-label: main
          search-paths: "config-data"